# Objective

## Begin Phase 1 — Foundations and Data Structure Implementation

---

## Role

You are a software implementation specialist responsible for translating the established roadmap, design references, and project context into the initial implementation phase. Approach this objective with careful attention to the existing application structure, data foundations, and established design patterns.

---

## Description

Begin implementing the roadmap from its designated starting point: **Phase 1 — Foundations and the Shape of the Data**. Use the existing project context and requirements documented in the `dANALYSIS - PROMPT ENHANCER` folder to understand the broader implementation direction and the work that needs to be completed. Treat this phase as the foundation for the subsequent roadmap phases, with particular attention to establishing the required underlying structure and data shape. Use the existing template implementation as the design and component reference while preserving it unchanged.

---

## Primary Objective

Implement **Phase 1 — Foundations and the Shape of the Data** as the starting point of the established roadmap, using the existing project context and template resources as implementation references.

---

## Constraints

* Never alter any files within `src\app\(template)`.
* Treat `src\app\(template)` strictly as a reference source for the designs, layouts, and components required for this implementation.

---

## Out of Scope

* Modifying, restructuring, replacing, or otherwise altering the contents of `src\app\(template)`.

---

## Context & Dependencies

* This task represents the starting point for implementing the broader roadmap.
* Review the `dANALYSIS - PROMPT ENHANCER` folder for the existing context, requirements, and information describing what needs to be implemented.
* The work must begin with **Phase 1 — Foundations and the Shape of the Data**, which serves as the foundation for the roadmap.
* The existing `src\app\(template)` directory provides the reference designs, layouts, and components that should guide the implementation.

---

## Supporting Tasks

### Project Context Review

* Review the `dANALYSIS - PROMPT ENHANCER` folder to understand the established project context, roadmap requirements, and expected implementation direction.
* Use the available information to guide the implementation without introducing requirements that are not already established.

### Phase 1 Implementation

* Begin implementation with **Phase 1 — Foundations and the Shape of the Data**.
* Establish the foundational structures and data shape required by this phase before progressing to later roadmap work.

### Template Reference

* Refer to `src\app\(template)` for the existing designs, layouts, and components relevant to the implementation.
* Preserve the template directory exactly as it is and use it only as an implementation reference.
